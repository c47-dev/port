{
  "project": "PortCheck Docker dual-pane",
  "stories": [
    {
      "id": "US-D1",
      "title": "Phase 1 — Update product spec",
      "priority": 1,
      "passes": true,
      "acceptanceCriteria": [
        "docs/spec/portcheck.md documents Local Port and Docker Port surfaces",
        "Spec states Docker segment hidden when Engine unavailable OR zero published TCP (OQ-6)",
        "Spec states PortCheck never starts Docker; Docker Kill = container stop; Kill All only in Local section",
        "rg \"Docker is not available\" docs/spec returns no matches"
      ]
    },
    {
      "id": "US-D2",
      "title": "Phase 2 — Services and ViewModel",
      "priority": 2,
      "passes": true,
      "acceptanceCriteria": [
        "Models DockerPortInfo, HostListenSnapshot, PortPane exist",
        "DockerEngineClient uses named pipe only (rg docker.exe src/PortCheck/Services returns no spawn of docker CLI)",
        "TrayViewModel exposes LocalPorts, DockerPorts, Filtered*, IsDockerSurfaceVisible, ActivePane",
        "IsDockerSurfaceVisible false when probe fails or catalog has zero published TCP rows",
        "dotnet build -c Debug in src/PortCheck exits 0"
      ]
    },
    {
      "id": "US-D3",
      "title": "Phase 3 — UI, kill flows, tray integration",
      "priority": 3,
      "passes": true,
      "acceptanceCriteria": [
        "TrayPopupWindow has Local Port / Docker Port segment; Docker segment Visibility bound to IsDockerSurfaceVisible",
        "Kill All control is inside Local Port section; footer has Refresh and Hide only",
        "Docker row shows port detail and Kill triggers KillContainerCommand",
        "Local rows show Docker badge when IsDockerPublished",
        "Compact row styles: GlassPortListItem Padding 8,3 and MinHeight 26 on local rows",
        "dotnet build -c Debug in src/PortCheck exits 0"
      ]
    },
    {
      "id": "US-D4",
      "title": "Phase 3 — Verification",
      "priority": 4,
      "passes": true,
      "acceptanceCriteria": [
        "dotnet build -c Release -r win-x64 in src/PortCheck exits 0",
        "rg \"Docker is not available\" src/ returns no matches",
        "docs/implementation-records/260527_docker-dual-pane-ports.md OQ-6 marked resolved: hide segment when zero publishes"
      ]
    }
  ]
}
