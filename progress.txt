# PortCheck Docker dual-pane — 2026-05-27

## Done
- Phase 1: docs/spec/portcheck.md — Local/Docker surfaces, gate (OQ-6), never start Docker, Kill All in Local section
- Phase 2: DockerEngineClient (named pipe), DockerPortCatalogService, DockerContainerStopService, DockerPortInfo, HostListenSnapshot, PortPane, TrayViewModel orchestration
- Phase 3: TrayPopupWindow dual segment + compact rows; Kill All moved out of footer; Docker Kill = stop; Docker badge on local rows
- IsDockerSurfaceVisible = probe OK AND catalog row count > 0 (no segment when zero publishes)
- dotnet build Debug + Release win-x64: pass

## Patterns
- Docker integration: passive pipe only; no docker.exe subprocess
- VM single-flight refresh; catalog fetch only when probe succeeds (gate uses catalog count)

## Manual QA (user)
- Docker off → popup shows Local only
- docker run -p HOST:CONTAINER → Docker segment appears with port detail rows
- Kill on Docker row stops container; Kill All in Local section only
